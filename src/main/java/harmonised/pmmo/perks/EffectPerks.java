package harmonised.pmmo.perks;

import org.apache.logging.log4j.util.TriConsumer;

import net.minecraft.nbt.CompoundTag;
import net.minecraft.server.level.ServerPlayer;

public class EffectPerks {

	public static TriConsumer<ServerPlayer, CompoundTag, Integer> NIGHT_VISION = (player, nbt, level) -> {
		
	};
	
	public static TriConsumer<ServerPlayer, CompoundTag, Integer> NIGHT_VISION_TERM = (player, nbt, level) -> {
		
	};
}
