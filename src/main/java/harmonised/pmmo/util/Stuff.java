package harmonised.pmmo.util;

import com.google.gson.Gson;
import com.google.gson.JsonObject;
import net.minecraft.item.Item;
import net.minecraft.util.JSONUtils;
import net.minecraftforge.registries.ForgeRegistries;

public class Stuff
{
//    final String contents = "a";// obtain them somehow
//    final JsonObject object = Gson.from(contents, JsonObject.class); // convert it with a wildcard
//    object.entrySet().forEach(it -> {
//    final String requirement = it.getKey();
//    if (!VALID_REQUIREMENTS.contains(requirement)) throw new IllegalStateException(requirement + " not valid");
//    final JsonObject affectedItems = JSONUtils.getJsonObject(it.getValue(), requirement);
//    affectedItems.entrySet().forEach(itemData -> {
//        final Item item = ForgeRegistries.ITEMS.getValue(new ResourceLocation(itemData.getKey()));
//        final JsonObject statistics = JSONUtils.getJsonObject(itemData.getValue(), itemData.getKey()));
//        statistics.entrySet().forEach(stat -> {
//            final String name = stat.getKey();
//            if (!VALID_STATISTICS.contains(name)) throw new IllegalStateException("idiot");
//            final int value = JsonUtilities.getInt(stat.getValue(), name);
//            // do shit
//            });
//        });
//    });
}
